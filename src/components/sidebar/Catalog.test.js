import React from 'react';
import ReactDom from 'react-dom';
import Catalog from './Catalog';

test('should appear a function called obtenerPhonesAction', () => {
    expect(obtenerPhonesAction())
});